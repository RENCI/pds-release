version: '3'
services:
  pdspi-guidance-demo-doac:
    image: pdspi-guidance-demo-doac:${pdspi_guidance_demo-doac_TAG}
    port: 8080
    environment:
      PDS_HOST: ${PDS_HOST}
      PDS_PORT: ${PDS_PORT}
      PDS_VERSION: ${PDS_VERSION}
