version: '3.4'
services:
  pds:
    port: 8080
    image: pds:${pds_TAG}
    environment:
      PDS_HOST: ${PDS_HOST} 
      PDS_PORT: ${PDS_PORT}
      PDS_VERSION: ${PDS_VERSION}
      PDS_CONFIG: ${PDS_CONFIG}
      PDS_LOGGING: ${PDS_LOGGING}
    
