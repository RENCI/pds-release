version: '3'
services:
  pdspi-guidance-example:
    image: pdspi-guidance-example:${pdspi_guidance_example_TAG}
    port: 8080
    environment:
      PDS_HOST: ${PDS_HOST}
      PDS_PORT: ${PDS_PORT}
      PDS_VERSION: ${PDS_VERSION}
